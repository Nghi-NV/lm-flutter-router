name: lm_flutter_router
description: "Typed adaptive Flutter routing with custom RouterDelegate navigation, guards, deep links, iOS-style transitions, and split-view projection."
version: 0.0.1
repository: https://github.com/Nghi-NV/lm-flutter-router
topics:
  - router
  - navigation
  - deeplink
  - adaptive
  - flutter

environment:
  sdk: ^3.11.1
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
